#include <stdio.h>

/**
 * main - this serves as the starting point of the program execution
 *
 * Return: always 0
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
