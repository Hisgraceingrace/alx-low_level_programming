#include "main.h"

/**
 * mul - multipies two integers
 * @x: first number to multiply
 * @y: second number to multiply
 * Return: the result of multiplication between two integers
 */

int mul(int x, int y)

{
	return (x * y);
}
